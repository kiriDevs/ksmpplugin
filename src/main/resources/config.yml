# Default config generated by ksmpplugin version ${version} (kiriSMP4)
# See documentation (all options, possible values, defaults, etc.) on GitHub:
# https://github.com/kiriDevs/ksmpplugin/blob/v${version}/docs/recipes.md

recipes:
  crafting:
    chainArmor: chains
    chain:
      cheap: false
      yield: 2
    bundle:
      chest: false
      shell: either
    saddle: true
    cheapLantern: true
    bell: true
  stonecutting:
    wood:
      slabs: true
      stairs: true